/**
 * Expo Push Notification SDK Initialization
 * TODO: Install expo-server-sdk and add EXPO_ACCESS_TOKEN to env when ready
 */

// Placeholder — uncomment when expo-server-sdk is installed
// import { Expo } from "expo-server-sdk";
// import { env } from "./env";
//
// const expo = new Expo({ accessToken: env.EXPO_ACCESS_TOKEN });
// export default expo;
